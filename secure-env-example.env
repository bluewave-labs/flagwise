# CRITICAL SECURITY SETTINGS - NEVER COMMIT TO REPO
# Generate secure keys using: python -c "import secrets; print(secrets.token_urlsafe(32))"

# JWT Security
JWT_SECRET_KEY=REPLACE_WITH_SECURE_32_CHAR_KEY_GENERATED_ABOVE
ENCRYPTION_KEY=REPLACE_WITH_SECURE_32_CHAR_KEY_GENERATED_ABOVE

# Database Security
POSTGRES_USER=secure_shadow_user
POSTGRES_PASSWORD=REPLACE_WITH_SECURE_PASSWORD
POSTGRES_DB=shadow_ai_prod

# Network Security
CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Kafka Security (if enabled)
KAFKA_ENABLED=true
KAFKA_BROKERS=secure-kafka-cluster:9093
KAFKA_USERNAME=shadow_ai_service
KAFKA_PASSWORD=REPLACE_WITH_SECURE_KAFKA_PASSWORD
KAFKA_AUTH_TYPE=SASL_SSL

# Optional: External Secret Management
# AWS_SECRETS_MANAGER_SECRET_NAME=shadow-ai/prod
# VAULT_URL=https://vault.yourdomain.com
# VAULT_TOKEN=hvs.SECURE_VAULT_TOKEN